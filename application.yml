server:
  port: 9527
spring:
  application:
    name: ZUUL-GATEWAY
  profiles: dev
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka
  instance:
    instance-id: gateway-9527
    prefer-ip-address: false
